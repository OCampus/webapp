import React from 'react';
import SmoothScroll from '@/components/SmoothScroll';
import { Navbar, Footer}  from '@/components/index'

const SmoothScrollWrapper = ({ children } : any) => {
  return (
        {children}
  );
};

export default SmoothScrollWrapper;